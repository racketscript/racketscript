#lang info

(define collection 'multi)
(define version "0.1")

(define deps
  (list "racket"
        "rapture-compiler"))

(define build-deps '())

(define pkg-authors '(vishesh))
(define pkg-desc "Extras to play with Rapture.")
